Creating a branch & switching to it 

git switch -c <nameOfBranch>
git log --decorate=no. You should see that the branch names are no longer shown at all.
git log --decorate=no. Show full log with branch
git log --oneline. gives a compact view
git log --oneline --graph --parents. This should give you a nice visual representation of the merge commit.